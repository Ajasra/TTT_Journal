#import "common.typ": *
#let sketchpad-page(idx, total) = {
  system-header("FREE-FORM SKETCHPAD / DRIFT FIELDS", "NOTE FIELD " + str(idx) + "/" + str(total))
  v(380pt) // dot grid covers background
}
